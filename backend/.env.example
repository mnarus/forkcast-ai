OPENAI_API_KEY=

# PostgreSQL settings for local development
POSTGRES_DB=forkcast_ai
POSTGRES_USER=forkcast
POSTGRES_PASSWORD=forkcast
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Set to true only if you want to temporarily fall back to SQLite.
USE_SQLITE=false
